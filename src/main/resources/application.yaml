server:
   port: 8081
   
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Ash_conversion?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Lyne@002#Assouh
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

app:
  upload:
    directory: uploads
    originals-directory: uploads/originals
  token:
    expiration: 3600
  